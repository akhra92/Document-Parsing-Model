# Manifest for Streamlit Community Cloud. The PyMuPDF trio is pinned to one
# exact version because pymupdf4llm requires it - keep it in step with
# pyproject.toml.
pymupdf==1.28.2
pymupdf4llm==1.28.2
pymupdf-layout==1.28.2
streamlit>=1.36
